<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:ExpressoReporting;assembly=ExpressoReporting"
             mc:Ignorable="d"
             Visual="Material"
             Title="{x:Static local:res.MnuLogin}"
             x:Class="ExpressoReporting.Views.LoginPage">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical" Margin="10">
            <Entry Placeholder="{x:Static local:res.Username}" PlaceholderColor="{StaticResource PrimaryColor}"
                   TextColor="{StaticResource SecondaryColor}"
                   x:Name="TxtUsername"/>
            <Entry Placeholder="{x:Static local:res.Password}" PlaceholderColor="{StaticResource PrimaryColor}"
                   TextColor="{StaticResource SecondaryColor}" IsPassword="True"
                   x:Name="TxtPassword"/>
            <Button Text="{x:Static local:res.MnuLogin}" HorizontalOptions="CenterAndExpand" Margin="0,20"
                    BackgroundColor="{StaticResource PrimaryColor}"
                    x:Name="CmdLogin" Clicked="CmdLogin_OnClicked" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>